export interface RoadmapPhase {
  id: string;
  phase: string;
  title: string;
  items: string[];
  status: string;
}

export interface FeatureCard {
  id: string;
  title: string;
  description: string;
  iconName: string;
}
